# Agent 224 Handoff — skill-mastery.js: FULL PASS, 6 new equivalents (1 third independent validate-after-sanitize tautology, 2 null-substitution-ternary repeats, 1 NaN-fallback repeat, 1 impossible-else-branch repeat, 1 unreachable post-sanitize re-check) + mastery-review-ui.js: FULL PASS, 4 new equivalents + 1 real-kill; gamification.js/review-scheduler.js flagged items formally resolved (both real kills) — against the CURRENT 954-test suite; no source or test file changed; 954 tests unchanged

## Context
Continued `HANDOFF_AGENT_223.md`'s "Next agent — start here" instruction #1: formally test the two grep-flagged null-substitution-ternary candidates (`gamification.js:147`, `mastery-review-ui.js:51`) before picking a new file, then followed instruction #1's file-selection guidance toward the smaller/denser remaining files rather than the largest (`offline-packs-ui.js`, 155 lines) first, and — once the `mastery-review-ui.js` flagged item turned into a full-file sweep — extended the session to `skill-mastery.js` (230 lines, previously 1/8 in the backlog) once mastery-review-ui.js was exhausted, since it shares the exact `validateStore`/`normalizeLevel` shapes already confirmed twice elsewhere and was the highest-confidence remaining target.

## Method (unchanged from Agents 213–223)
Hand-apply a candidate mutation, run the full `tests/run.js` suite, check whether the only failure is the `offline/packs/core.zip ... byte-identical` identity check (true false-kill for core-manifest files only), then revert and re-confirm the 954-passed baseline before trying the next candidate. Baseline (954/0) confirmed at session start and re-confirmed after every mutation.

## Findings

### 2 flagged items formally resolved (both REAL KILLS)
| Location | Mutation tried | Result |
|---|---|---|
| `gamification.js:147` `recordSession` | Changed `meta.quizId == null ? '' : String(meta.quizId).trim()` to `String(meta.quizId).trim()` | **951 passed, 3 failed → REAL KILL, confirmed.** A missing `quizId` must stay `''` (falsy) so `rewardId` stays `''`; without the guard, `String(null)`/`String(undefined)` ('null'/'undefined') are truthy and a reward-ledger key gets created for a quiz that was never identified. |
| `mastery-review-ui.js:51` `esc` | Changed `String(value == null ? '' : value)` to `String(value)` | **953 passed, 1 failed (identity only) → equivalent, confirmed.** `esc` is only ever called on values already coerced to strings/numbers by the render code above it (labels, ids, accuracy text); no fixture calls `esc(null)`/`esc(undefined)` directly, so the branch that would print the literal text "null"/"undefined" is currently dead code from the test suite's perspective — not dead by construction, just uncovered. See the coverage-gap note below. |

Both reverted immediately after confirming.

### mastery-review-ui.js: full pass, 4 new equivalents + 1 real-kill (in addition to `esc` above)

| Location | Mutation tried | Result | Why |
|---|---|---|---|
| `labelForSkill` (~57) | `String(skill \|\| '')` → `String(skill)` | **953/1 (identity only) → equivalent** | Every call site passes a skill name that is either a literal `Object.keys(...)` result or a value already validated by `normalizeSkill`/`normalizeLevel` upstream (in `skill-mastery.js`/`review-scheduler.js`) — never `null`/`undefined`/`0`/`false` in this codebase's actual call graph, so the `\|\| ''` guard's only distinguishing inputs are never reached. |
| `bandLabel` (~67) | `return key ? String(key).replace(...) : 'Tracked';` → `return String(key).replace(...);` | **952 passed, 2 failed → REAL KILL, confirmed.** An untracked/no-band skill (`key` undefined) must show `'Tracked'`, not the literal text `'undefined'`. **Do not re-try.** |
| `buildSummary`'s `totalQuestions` (~80) | `Number(skills[skill].question_count \|\| 0)` → `Number(skills[skill].question_count)` | **953/1 (identity only) → equivalent** | `tracked` (the array being reduced) is pre-filtered to only skills where `question_count > 0`, so `question_count` is guaranteed truthy for every element reaching this line — the `\|\| 0` fallback is guaranteed-by-construction unreachable. |
| `render`'s mastery-row question count (~186, both occurrences on the same line) | `Number(data.question_count \|\| 0)` (×2, in the same template-string line) → `Number(data.question_count)` (×2) | **953/1 (identity only) → equivalent** | Same guarantee as above — this line runs inside the `summary.tracked` loop, so `data` is always a tracked skill with `question_count > 0`. Same class as the `totalQuestions` finding, second instance in the same file. |

`bandLabel`'s real-kill reverted immediately; the three equivalents reverted after confirming each.

### skill-mastery.js: full pass, 6 new equivalents + 2 real-kills

| Location | Mutation tried | Result | Why |
|---|---|---|---|
| `validateStore` (~199–209) | Replaced the whole `SKILLS.every(...)` body with `return true;` (keeping the `sanitizeStore(store)` call and the preceding version check) | **953/1 (identity only) → equivalent** | Same shape as `recommendations.js`'s `validateProfile` (Agent 221) and `review-scheduler.js`'s `validateStore` (Agent 222): `sanitizeSkill` already `clamp`s `correct_count` to `[0, question_count]`, produces `attempt_count`/`level_counts` entries only via `countOrZero` (always `>= 0`, and `level_counts` entries are only ever written when `count > 0`), and only sets `accuracy` (always `[0,100]` by construction, since it's a rounded `correct/question` percentage with `correct_count <= question_count`) when `question_count > 0`. All four `.every()`/nested-`.every()` conditions are guaranteed true for every skill that could reach this check. **This is the THIRD independent instance of this exact tautology class, now confirmed in three separate files** — strong enough to treat as an established, no-longer-novel pattern rather than a per-file curiosity. |
| `validateStore`'s own version re-check (~202, immediately above the block just described) | Removed `if (s.version !== VERSION) return false;` (keeping the `var s = sanitizeStore(store);` line and the `return SKILLS.every(...)` that follows) | **953/1 (identity only) → equivalent** | `sanitizeStore`'s early-return path (`if (!raw \|\| ... \|\| Number(raw.version) !== VERSION) return emptyStore();`) returns `emptyStore()`, and `emptyStore().version` is unconditionally `VERSION`; the non-early-return path builds `result = emptyStore()` and never reassigns `result.version` either. So `sanitizeStore(...)` **always** returns an object whose `.version` is `VERSION`, making the caller-side re-check of `s.version` unreachable by construction — distinct from the `SKILLS.every` tautology above (that one re-checks bounds the sanitizer enforces; this one re-checks an invariant the sanitizer can't help but satisfy, a new sub-class: **re-verifying a constant the sanitizer's return type itself guarantees**). |
| `normalizeSkill` (~20) | `String(skill \|\| '')` → `String(skill)` | **953/1 (identity only) → equivalent** | Same reasoning as Agent 221/222's `normalizeLevel`-shaped findings: `String(undefined)`/`String(null)` produce `'undefined'`/`'null'`, neither of which is ever in `SKILLS`, so the final indexOf-driven found/not-found outcome (and thus the `null` return) is unchanged. Confirms the class generalizes beyond the `== null ? '' : x` ternary shape to the `x \|\| ''` shape too. |
| `normalizeLevel` (~24) | `String(level \|\| '')` → `String(level)` | **953/1 (identity only) → equivalent** | Identical reasoning and identical shape to `normalizeSkill` above, in the same file — second instance in the same session, third+fourth overall counting `normalizeLevel` findings in `recommendations.js`/`review-scheduler.js`. |
| `masteryBand` (~50) | `clamp(Number(accuracy) \|\| 0, 0, 100)` → `clamp(Number(accuracy), 0, 100)` | **953/1 (identity only) → equivalent** | Directly confirmed by an existing test (`masteryBand('junk').key === 'needs_support'`): with the `\|\| 0` removed, `Number('junk')` is `NaN`, and `clamp(NaN, 0, 100)` is also `NaN` (via `Math.min`/`Math.max` propagating `NaN`); the loop below initializes `result = MASTERY_BANDS[0]` and only overwrites it when `n >= MASTERY_BANDS[i].min`, which is false for every comparison against `NaN` — so `result` stays `MASTERY_BANDS[0]` (`needs_support`) either way. **New class: a `\|\| 0` NaN-guard whose fallback value happens to coincide with what an unguarded `NaN` already produces one level up (in the caller's loop), rather than the guard being consumed directly.** |
| `recordAttempt`'s per-skill `accuracy` line (~178) | `item.question_count ? Math.round(...) : null` → unconditional `Math.round(...)` | **953/1 (identity only) → equivalent** | Exact structural match to Agent 222's `review-scheduler.js` `recordAttempt` finding: this line runs inside `Object.keys(contributed).forEach(...)`, and `contributed[skill]` is only ever set to `true` in the same function, in the same pass, immediately after `item.question_count += 1` — so every skill reaching this line has `question_count >= 1` by construction, and the `: null` branch is unreachable. |

Both real-kills below reverted immediately after confirming; all equivalents reverted after confirming each.

### Real-kill candidates confirmed (mutation-tested)
- `confidenceFor`'s two `Number(x) || 0` fallbacks (`var q = Number(questionCount) || 0; var a = Number(attemptCount) || 0;`): removing both → **952 passed, 2 failed → confirmed real kill.** With the fallback removed, a non-numeric `questionCount` (→ `NaN`) makes `q < 5` false (not true), which can flip the confidence tier when `attemptCount` alone doesn't already resolve it. **Do not re-try.**
- `sanitizeSkill`'s `last_quiz_id` null-guard (`raw.last_quiz_id == null ? null : String(raw.last_quiz_id)`): removing the ternary → **952 passed, 2 failed → confirmed real kill.** A genuinely absent `last_quiz_id` must stay `null`, not become the string `'null'`/`'undefined'`. **Do not re-try.**

### Swept this session — no further candidates found
- `countOrZero`, `timeOrNow`, `finiteOrNull`, `clamp`: re-read; these carry Agent 201/158's own comments explaining exactly the `Number(x)`-coercion traps they guard against (`Number('Infinity')`, `Number(null)`, blank strings) and were not re-tested given the comments already document confirmed real-kill reasoning from those agents.
- `sanitizeSkill`'s `level_counts` loop, `sanitizeStore`, `readStored`, `writeStored`, `clone`, `recordAndPersist`, `getSkill`: each re-read; no further redundant guard, same-value reassignment, unreachable branch, or impossible-state guard found beyond what's documented above.
- `offline-packs-ui.js` (155 lines): read in full. No confirmed candidates found on inspection — the file's guards (the `esc()` type-check, the pack-id filter's `Number.isFinite`, `progress.total || pack.files.length || 1`) each visibly correspond to a distinct malformed-input shape the file's own comments call out (Agent 204's object/throwing-toString note). Not mutation-tested this session (time budget went to skill-mastery.js instead, which had a much clearer match to already-confirmed classes); flagged for a future agent's first pass rather than left unmentioned.

## Tests: 954 -> 954 (no change; this pass only ran the existing suite against temporary, reverted mutations)

## Cache / pack
No source file changed. `CACHE_VERSION` unaffected — none of `gamification.js`, `mastery-review-ui.js`, `skill-mastery.js`, `offline-packs-ui.js` are part of the cache manifest. Full suite confirmed 954 passed / 0 failed with the tree byte-identical to Agent 223's zip (`diff -rq` against Agent 222's original extracted tree confirmed clean per-file at session end for every file touched this session, excluding the two handoff files).

## Files changed vs Agent 223 zip
None (only `HANDOFF_AGENT_224.md` added).

## Not done / carried forward
- Item 1(a) backlog progress (cumulative across Agents 213–224): placement.js (25/51), recommendations.js (fully swept, Agent 221), review-scheduler.js (fully swept + its one carried item now confirmed, Agents 222–223), runtime-v2-adapter.js (fully swept, Agent 223), **mastery-review-ui.js is now fully swept (5 confirmed equivalents total across this session + Agent 223's flagged item, 1 confirmed real-kill; no remaining open items)**, **skill-mastery.js is now fully swept (6 confirmed equivalents, 2 confirmed real-kills; no remaining open items)**. Remaining backlog: `offline-packs-ui.js` (read but not mutation-tested this session — good next candidate, small file), `offline-packs.js` (244 lines, not touched this session), and the rest of `placement.js` (26/51 remaining).
- Same limitation as Agents 213–223: no access to the original per-file handoff text (203/204/207/208/209/210) this session; verification continues from first principles rather than re-reading the original arguments.
- Decision-gated backlog: unchanged (see Agent 203/210/211/212/213 notes for the full list). The `validateProfile`/`validateStore` scope question (Agents 221/222) now has a THIRD independent instance (`skill-mastery.js`'s `validateStore`) — worth raising to a product owner as "these three validators, across three files, never actually catch anything their own file's sanitizer hasn't already guaranteed; are they meant to catch corruption from a source other than each file's own sanitizer (e.g. a manually-edited localStorage value bypassing the sanitizer entirely), in which case they're doing their job by construction and should stay, or are they dead weight?" This is now a substantive, cross-file product question rather than a single-file curiosity.
- Two candidate files with the null-substitution-ternary shape from Agent 223's grep have both now been resolved (`gamification.js:147` real kill, `mastery-review-ui.js:51` equivalent) — no more outstanding grep hits of this shape are known; a future agent could re-grep the untouched files (`offline-packs.js`, `placement.js`'s unswept portion) for the same shape as a fast first move.
- `tools/mutate.js` still not checked into the shipped tree (Agent 212's suggestion, still open).

## Next agent — start here
1. `mastery-review-ui.js` and `skill-mastery.js` are now fully swept — **do not re-try** any of the confirmed equivalents or real-kills in this handoff or Agents 220–223's. Recommend `offline-packs-ui.js` next (small, already read this session with no obvious candidates spotted on inspection — a formal mutation pass would confirm that rather than leave it as an inspection-only claim) or `offline-packs.js` (244 lines, completely untouched).
2. Four recurring classes are now confirmed in *three or more separate files each* and should be treated as established, not novel, when found again: (a) **validate-after-sanitize tautology** (now 3 files: placement.js, recommendations.js, review-scheduler.js, skill-mastery.js — actually 4), (b) **null-substitution guard in a level/id/skill normalizer feeding a fixed-list/indexOf check**, seen in both the `== null ? '' : x` ternary shape and the `x || ''` shape (now confirmed in recommendations.js, review-scheduler.js×1, skill-mastery.js×2), (c) **re-wrapping/re-stringifying an argument that every call site already supplies in the target type** (runtime-v2-adapter.js's `idOf`, mastery-review-ui.js's `labelForSkill`), (d) **a guard against an accumulator/loop-derived state made structurally impossible by earlier code in the same function** (review-scheduler.js's `recordAttempt`, skill-mastery.js's `recordAttempt` — same function name, same shape, two different files).
3. New this session, worth watching for: (e) **a `|| 0`/`|| fallback` guard whose fallback value happens to coincide with what the unguarded value already produces downstream** (skill-mastery.js's `masteryBand`, where an unguarded `NaN` and the guarded `0` both land on the same loop-initial result) — subtler than class (b)/(c) because the guard's *own* line looks load-bearing in isolation; only tracing the value one level further (into the caller's loop or filter) reveals the equivalence. (f) **a caller-side re-check of an invariant a callee's return type already guarantees** (skill-mastery.js's `validateStore` re-checking `s.version` after `sanitizeStore` already guarantees it) — distinguish this from class (a): class (a) re-checks *bounds* the sanitizer enforces on *data*; class (f) re-checks a *constant* the sanitizer's own return shape can't violate.
4. Coverage-gap note (not a bug, not urgent, just worth carrying forward so a future agent doesn't re-derive it): `esc()`-style HTML-escaping helpers in this codebase (`mastery-review-ui.js`'s `esc`) are only ever called on already-typed values by every current fixture, so their `null`/`undefined`-substitution guards read as "equivalent" against the current suite even though they're clearly intentional defensive code, not dead code. If a future agent is tempted to recommend simplifying these based on this session's equivalence findings, that would be wrong — these are coverage gaps, not redundant code, and the distinction matters for any refactor decision.
5. Reusable per-file recipe (unchanged): `cp` the file → mutate one candidate with a `str_replace`/python replace → `node tests/run.js` → if the *only* failure is the core.zip identity line (core-manifest files only, check `offline/core-manifest.json`) it's not a real kill, i.e. still equivalent → restore → next candidate. If more than the identity line fails, it's a real kill — not equivalent — revert and move on.
6. If picking up the decision-gated backlog instead, it is unchanged and ready — see the validate-after-sanitize product question above (now backed by 3 independent file instances) for the strongest candidate to actually raise with a product owner this session, if one is available.

## Blockers
None new. Carried forward: product decisions for the DECISION backlog, now including the strengthened (3-instance) validate-after-sanitize scope question; a real device for Agent 203's item 17; the `normalizeQuestion`-throws-on-malformed-question observation (product/refactor call); lack of access to Agents 202-210's original handoff text (noted since Agent 213, still true this session).

## Artifacts
`mylingo-v191-agent224-equivalent-sweep.zip` (byte-identical contents to Agent 223's zip except this handoff file — no source/test changes this pass), `HANDOFF_AGENT_224.md`.

## Resume command
"Resume from HANDOFF_AGENT_224.md. You are Agent 225. Continue from 'Next agent — start here'."

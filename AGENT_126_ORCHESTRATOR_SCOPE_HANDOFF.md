# Agent 126 — Orchestrator Scope Handoff

## Status
COMPLETED

## Mission
Translate the requested UI/UX fixes into bounded, independent agent lanes so free-tier agent interruptions do not lose context.

## Scope frozen
1. Bottom navigation dead links / unnecessary Practice tab.
2. Quiz descriptions on quiz cards.
3. Stronger dark-mode contrast.
4. Quizzes rendered as activities inside lessons, not as a disconnected lesson destination.
5. Standard app splash screen.
6. Simplify the home screen to essential actions only.
7. Preserve offline/PWA contracts and run static verification after every lane.

## Handoff rule
Every following agent receives this ZIP plus the latest handoff. No agent should infer prior work from conversation history.

## Exact next tasks
- Agent 127: navigation + link integrity.
- Agent 128: lesson/quiz integration + quiz-card descriptions.
- Agent 129: visual theme + splash.
- Agent 130: home simplification + cross-page consistency.
- Agent 131: offline/PWA reconciliation + release verification + packaging.

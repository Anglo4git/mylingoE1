# Data Structure Orientation (Reference Only)

This folder holds raw source data used to derive the canonical schema:
sample dataset, error-avoidance notes, and the master source CSV.

The duplicate `mylingo-admin (authoring app).html` previously stored here
was a stale, pre-Rich-Question-Authoring snapshot of the admin app and has
been removed (Agent 74). The single canonical authoring app is
`authoring/mylingo-admin.html` — see `authoring/README.md`.

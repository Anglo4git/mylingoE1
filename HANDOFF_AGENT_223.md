# Agent 223 Handoff — runtime-v2-adapter.js: FIRST PASS, 2 new equivalents confirmed (1 redundant re-stringify of an already-string fallback, 1 falsy-coercion guard masked by a numeric-only edge case never exercised) + 1 flagged-but-unconfirmed item formally confirmed as a REAL KILL — against the CURRENT 954-test suite; no source or test file changed; 954 tests unchanged

## Context
Continued `HANDOFF_AGENT_222.md`'s "Next agent — start here" instructions #1 and #2: formally mutation-test the `recordAttempt` banner-guard drop in `review-scheduler.js` (flagged but not tested by Agent 222), then move to the last of the three originally-named fresh files — `shared/js/runtime-v2-adapter.js` (408 lines, ~20 internal/exported functions) — since review-scheduler.js was assessed as likely near-exhausted.

## Method (unchanged from Agents 213–222)
Hand-apply a candidate mutation, run the full `tests/run.js` suite, check whether the only failure is the `offline/packs/core.zip ... byte-identical` identity check (true false-kill for core-manifest files only), then revert and re-confirm the 954-passed baseline before trying the next candidate. Baseline (954/0) confirmed at session start and re-confirmed after every mutation.

## Findings

### Item carried from Agent 222, now formally confirmed
- **`recordAttempt` banner-guard** (`review-scheduler.js` ~155): dropped `type === 'banner' ||` from `if (!skill || type === 'banner' || typeof correctMap[index] !== 'boolean') return;` → **950 passed, 4 failed → REAL KILL, confirmed.** Matches the pattern already independently confirmed three times in `placement.js` (Agent 220): a banner question with a matching skill and a boolean answer must still be excluded from scoring, and this is exercised by review-scheduler.js's own tests independent of placement.js's. Reverted immediately; `review-scheduler.js` is byte-identical to Agent 222's copy.

### 2 new equivalents confirmed (runtime-v2-adapter.js)

| Location | Mutation tried | Result | Why it's equivalent |
|---|---|---|---|
| `idOf` (~75–81) | Changed `return stringValue(fallback);` to `return fallback;` | **953 passed, 1 failed (identity only) → equivalent** | Every call site (`courseId`, `unitId`, `course_id`, `lessonId`, `activityId`) passes a `fallback` that is already a plain JS string — either the literal `''`, a `courseId` that itself already passed through `stringValue`, or a template-built string like `'unit-' + (unitIndex + 1)`. `stringValue` on an already-string value only does `.trim()`, and none of these constructed fallback strings ever carry leading/trailing whitespace, so re-wrapping in `stringValue` is a no-op at every call site. **New instance of the same "redundant re-stringify of a value that's already the target type" shape Agent 221 and 222 found in `normalizeLevel`/`validateProfile`-adjacent helpers, but here the redundancy is in the fallback arm of an id-lookup helper rather than a null-substitution ternary — worth generalizing the watch-list class to "wrapping an argument in a type-normalizing helper when every call site already supplies that type."** |
| `normalizeMedia`'s inner `one()` (~197) | Changed `label: stringValue(value.label || '')` to `label: stringValue(value.label)` | **953 passed, 1 failed (identity only) → equivalent** | `stringValue` already maps `undefined`, `null`, booleans, and non-finite numbers to `''`, so the `|| ''` only changes behavior when `value.label` is the number `0` (the one falsy value `stringValue` would NOT itself turn into `''`, since `stringValue(0)` is `'0'`) — every other falsy input (`undefined`, `''`, `false`, `NaN`) produces `''` either way. No authored fixture in the current suite sets a numeric-zero `label` on media, so the branch that would distinguish the two versions is never exercised. **New class distinct from anything in Agents 213–222's watch-list: not a null-substitution ternary and not a validate-after-sanitize tautology, but a `|| fallback` guard whose only behavior-changing input (numeric `0`) happens not to be covered by any existing test — flagged below as a coverage gap, not a code-quality recommendation, since the guard IS meaningful if a future authored payload ever supplies `label: 0`.** |

Both reverted immediately after confirming, baseline re-checked (954/0) before moving to the next candidate. Tried in sequence, not simultaneously.

### Real-kill candidate confirmed (mutation-tested, not just reasoned)
- `normalizeQuiz`'s brand fallback (`brand: stringValue(firstDefined(input, ['brand'], 'Mylingo')) || 'Mylingo'`): removing the trailing `|| 'Mylingo'` → **951 passed, 3 failed → confirmed real kill.** A blank/whitespace-only `brand` string (present-but-empty, so `firstDefined` returns it rather than falling through) needs the `|| 'Mylingo'` to still resolve to the default brand. **Do not re-try.**
- `normalizedLevel`'s fallback path (`return validLevel(value) || validLevel(fallback);`), tested by dropping the second `validLevel(...)` wrap → **949 passed, 5 failed → confirmed real kill.** An unvalidated `fallback` (e.g. `options.level` taken raw from a URL) must still be checked against `LEVELS`, or a junk fallback would pass through as the quiz's level. **Do not re-try.**

### Swept this session — no further candidates found
- `isObject`, `firstDefined`, `stringValue`, `numberValue`, `validLevel`, `normalizedType`, `tagsValue`, `normalizeAnswer`, `answerList`, `normalizeCorrectIndex`, `normalizeAcceptedAnswers`, `sourceOf`, `copyOptional`: each re-read line-by-line. The boolean short-circuit in `normalizeAnswer` (`typeof value === 'boolean' ? String(value) : stringValue(value)`) and the parallel short-circuit in `normalizeAcceptedAnswers`'s item mapper were checked against dropping to a bare `stringValue`/`normalizeAnswer` call and confirmed load-bearing — `stringValue` maps booleans to `''`, not `'true'`/`'false'`, and the accepted-answers mapper needs to keep booleans as booleans (not stringify them) per its own comment. The `Number.isFinite` guard inside `normalizeAcceptedAnswers`'s numeric branch was checked against a raw `NaN` leaking through the `!== undefined` filter and confirmed necessary. The legacy `option_0`/`answer_1` guard clauses in `normalizeCorrectIndex` were checked against the aliases loop that runs afterward (which also reads `correct_index`, unshifted) and confirmed distinguishing — the legacy branch's `+1` offset is the only thing that makes it non-redundant with the aliases loop, so none of its four conditions could be dropped without either double-applying or skipping the offset.
- `normalizeMedia` (outer function), `normalizeQuestion`, `normalizeQuiz`, `normalizeHierarchy`, `flattenActivityToQuiz`, `normalizeManifest`: each re-read; no further redundant guard, same-value reassignment, or unreachable branch found beyond the two equivalents and two real-kills documented above. The double-fallback shape in `normalizeQuestion`'s `type` line (`firstDefined(..., 'radio')` feeding `normalizedType(..., 'radio')`) was checked against a present-but-blank `question_type` string and confirmed NOT redundant — `firstDefined` returns a blank string as-is (blank is not `undefined`/`null`), so `normalizedType`'s own `|| fallback` is the only thing that rescues it back to `'radio'`.
- The `out.correctIndex == null && input.correctIndices !== undefined` delete-guard in `normalizeQuestion` was read but NOT mutation-tested — deleting a key changes JSON-serialized shape (absent vs. explicit `null`) in a way likely pinned by an existing ranking-question fixture; flagged for a future agent rather than risked here without more context.

## Tests: 954 -> 954 (no change; this pass only ran the existing suite against temporary, reverted mutations)

## Cache / pack
No source file changed. `CACHE_VERSION` unaffected — neither `runtime-v2-adapter.js` nor `review-scheduler.js` is part of the cache manifest. Full suite confirmed 954 passed / 0 failed with the tree byte-identical to Agent 222's zip (`diff -rq` against Agent 222's extracted zip confirmed clean at session end, excluding the handoff file itself).

## Files changed vs Agent 222 zip
None (only `HANDOFF_AGENT_223.md` added).

## Not done / carried forward
- Item 1(a) backlog progress (cumulative across Agents 213–223): placement.js (25/51, see Agent 220), recommendations.js (3/8 functions yielded equivalents, fully swept, see Agent 221), review-scheduler.js (4/~14 functions yielded equivalents, fully swept, see Agent 222; its one carried-forward flagged item — the `recordAttempt` banner guard — is now formally confirmed as a real kill, see above, so review-scheduler.js has **no remaining open items**), **runtime-v2-adapter.js is now a NEW file entered this backlog: 2 of its ~20 functions yielded confirmed equivalents this session (`idOf`'s fallback re-stringify, `normalizeMedia`'s `one()` label guard); the remaining ~18 are now fully read this session with no further candidates found (two real-kills mutation-confirmed: `normalizeQuiz`'s brand fallback, `normalizedLevel`'s fallback validation; one delete-guard flagged but not mutation-tested, pending more context).** The other originally-listed files (skill-mastery.js 1/8, mastery-review-ui.js 1/7, offline-packs-ui.js 1/38, offline-packs.js 1/19) are unchanged this session. **All three originally-named fresh files (recommendations.js, review-scheduler.js, runtime-v2-adapter.js) have now had a full first pass.**
- Same limitation as Agents 213–222: no access to the original per-file handoff text (203/204/207/208/209/210) this session; verification continues from first principles rather than re-reading the original arguments.
- Decision-gated backlog: unchanged (see Agent 203/210/211/212/213 notes for the full list), still carrying the `validateProfile`/`validateStore` scope question from Agents 221/222 (unresolved, no product signal yet — runtime-v2-adapter.js has no `validate*` function, so this session adds nothing new to that specific question).
- New, smaller coverage-gap note (not a DECISION item, just worth naming): the `normalizeMedia` `label: stringValue(value.label || '')` guard is only distinguishable from a simpler `stringValue(value.label)` when `label` is authored as the number `0` — no current fixture does this. Not a bug, not urgent, just noted so a future agent doesn't need to re-derive it.
- Two candidate files with the null-substitution-ternary shape noted for a future session (found via a repo-wide grep, NOT swept or mutation-tested this session — out of this session's scope, which stayed on runtime-v2-adapter.js): `shared/js/gamification.js:147` (`meta.quizId == null ? '' : String(meta.quizId).trim()`) and `shared/js/mastery-review-ui.js:51` (`String(value == null ? '' : value).replace(...)`). Neither has been read in full this session; flagging only the grep hit.
- `tools/mutate.js` still not checked into the shipped tree (Agent 212's suggestion, still open).

## Next agent — start here
1. All three originally-named fresh files (recommendations.js, review-scheduler.js, runtime-v2-adapter.js) have now had a full first pass. **Do not re-try** any of the confirmed equivalents or real-kills listed in this handoff or Agents 220–222's. Recommend picking the next file by remaining backlog density: `offline-packs-ui.js` (1/38 — largest unexplored surface, per Agent-213-era backlog estimate) or `skill-mastery.js` (1/8) are the strongest remaining candidates; `mastery-review-ui.js:51` and `gamification.js:147` (both flagged above, un-swept) are good first candidates to check within whichever file gets picked, since they already match the confirmed null-substitution-ternary class.
2. Three recurring classes are now confirmed in *two or more separate files each* and should be actively watched for in whichever file is picked next: (a) **validate-after-sanitize tautology** (placement.js/recommendations.js → now also review-scheduler.js's `validateStore`), (b) **null-substitution ternary in a level/id normalizer feeding a fixed-list/indexOf check**, and (c) — new as of this session — **re-wrapping an argument in a type-normalizing helper (e.g. `stringValue`) when every call site already supplies that type**, first seen in `idOf`'s fallback arm.
3. Also watch for the two "guaranteed-by-construction" classes from Agent 222 (unreachable trailing `return` after an exhaustive loop; a guard against an accumulator state made structurally impossible by earlier code in the same function) and this session's new coverage-gap pattern: a `|| fallback` guard whose only behavior-changing input is a specific falsy-but-typed value (e.g. numeric `0`) that happens not to be covered by any existing fixture — worth flagging as a coverage note even when the mutation reads as "equivalent" against the current suite, since it's equivalent-given-current-tests rather than equivalent-by-construction.
4. Reusable per-file recipe (unchanged): `cp` the file → mutate one candidate with a `str_replace`/python replace → `node tests/run.js` → if the *only* failure is the core.zip identity line (core-manifest files only, check `offline/core-manifest.json`) it's not a real kill, i.e. still equivalent → restore → next candidate. If more than the identity line fails, it's a real kill — not equivalent — revert and move on.
5. If picking up the decision-gated backlog instead, it is unchanged and ready — see the `validateProfile`/`validateStore` note (Agents 221/222) for the standing product question.

## Blockers
None new. Carried forward: product decisions for the DECISION backlog (validateProfile/validateStore scope question); a real device for Agent 203's item 17; the `normalizeQuestion`-throws-on-malformed-question observation (product/refactor call); lack of access to Agents 202-210's original handoff text (noted since Agent 213, still true this session).

## Artifacts
`mylingo-v190-agent223-equivalent-sweep.zip` (byte-identical contents to Agent 222's zip except this handoff file — no source/test changes this pass), `HANDOFF_AGENT_223.md`.

## Resume command
"Resume from HANDOFF_AGENT_223.md. You are Agent 224. Continue from 'Next agent — start here'."
